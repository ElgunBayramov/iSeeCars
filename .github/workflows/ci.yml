name: .NET CI + Docker

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore iSeeCars.API/iSeeCars.API.csproj

      - name: Build
        run: dotnet build iSeeCars.API/iSeeCars.API.csproj --no-restore

      - name: Publish
        run: dotnet publish iSeeCars.API/iSeeCars.API.csproj -c Release -o iSeeCars.API/bin/Release/net8.0/publish

      - name: Build Docker
        run: docker build -t iseecars-api .
